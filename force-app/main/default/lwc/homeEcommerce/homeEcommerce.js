import { LightningElement,wire,track } from 'lwc';
// import getAllProducts from '@salesforce/apex/EcommerceWebsiteBuilder.getAllEcomProducts'

export default class HomeEcommerce extends LightningElement {
    /* @track getListOfProducts 

    @wire(getAllProducts) 
    wiredGetProducts({data,error}){
        if(data){
            this.getListOfProducts = data
        }else if (error){
            console.log("error occured"+error)
        }
    } */
}